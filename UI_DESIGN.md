# UI 设计文档 · Three.js Shader 案例库

## 1. 设计理念

**关键词**：暗色沉浸、玻璃质感、克制留白、聚焦内容

面向开发者和创意工程师的 Shader 作品集。视觉上以暗色为主基调，让案例预览成为视觉焦点。使用毛玻璃（glassmorphism）和微妙的渐变提升质感，避免过于花哨的装饰。

---

## 2. 色彩系统

### 主色调

| Token | 色值 | 用途 |
|-------|------|------|
| `--bg-primary` | `#09090d` | 页面主背景 |
| `--bg-secondary` | `#12121a` | 卡片、面板背景 |
| `--bg-tertiary` | `#1a1a28` | 悬浮态、高亮区 |
| `--bg-glass` | `rgba(255,255,255,0.03)` | 毛玻璃底层 |

### 文本色

| Token | 色值 | 用途 |
|-------|------|------|
| `--text-primary` | `#f0f0f5` | 标题、重要文本 |
| `--text-secondary` | `#9090aa` | 正文、描述 |
| `--text-muted` | `#585870` | 辅助信息 |

### 强调色

| Token | 色值 | 用途 |
|-------|------|------|
| `--accent` | `#6c5ce7` | 主强调（紫） |
| `--accent-glow` | `rgba(108,92,231,0.25)` | 发光阴影 |
| `--accent-2` | `#00d2ff` | 次强调（青） |
| `--accent-3` | `#ff6b6b` | 警告/高亮 |

### 边框

| Token | 色值 | 用途 |
|-------|------|------|
| `--border` | `rgba(255,255,255,0.06)` | 默认边框 |
| `--border-hover` | `rgba(255,255,255,0.14)` | 悬浮边框 |

---

## 3. 字体系统

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| H1 | 40px / 1.2 | 700 | 主页大标题 |
| H2 | 22px / 1.3 | 600 | 案例标题、区块标题 |
| H3 | 16px / 1.4 | 600 | 卡片标题 |
| Body | 14px / 1.7 | 400 | 正文 |
| Caption | 12px / 1.5 | 400 | 辅助文字、标签 |

字体栈：`'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`

---

## 4. 间距系统

基于 4px 网格：

| Token | 值 | 用途 |
|-------|---|------|
| `--space-xs` | 4px | 紧凑间距 |
| `--space-sm` | 8px | 标签内边距 |
| `--space-md` | 16px | 卡片内边距 |
| `--space-lg` | 24px | 区块间距 |
| `--space-xl` | 40px | 大区块间距 |
| `--space-2xl` | 64px | 首屏留白 |

---

## 5. 首页设计

```
┌──────────────────────────────────────────────────────┐
│                                                      │
│                  ✦ Three.js Shader                   │
│                    案例实验室                          │
│            GPU 驱动的交互式视觉效果集合                │
│                                                      │
│  ┌──────────────────┐  ┌──────────────────┐         │
│  │  ┌────────────┐  │  │                  │         │
│  │  │  预览占位   │  │  │       ...        │         │
│  │  │    🌊      │  │  │                  │         │
│  │  └────────────┘  │  │                  │         │
│  │  水波涟漪         │  │   更多案例        │         │
│  │  Morlet 小波...   │  │   即将添加        │         │
│  │  [标签] [标签]    │  │                  │         │
│  └──────────────────┘  └──────────────────┘         │
│                                                      │
│                  © 2025 · Built with Three.js         │
└──────────────────────────────────────────────────────┘
```

- **Header**：大标题 + 副标题，垂直居中偏上，背景添加微弱的径向渐变光晕
- **卡片网格**：响应式，2~3 列，卡片为毛玻璃材质
- **卡片**：
  - 圆角 12px，半透明背景
  - 顶部为 16:9 预览缩略图（渐变色 + Emoji/图标）
  - 底部为标题 + 描述 + 标签
  - Hover：边框变亮、轻微上浮（translateY）、发光阴影
- **Footer**：简洁版权，文字次要色

---

## 6. 案例页设计

```
┌──────────────────────────────────────────────────────┐
│  ← 案例列表    水波涟漪 · Morlet Wavelet Ripple       │
├──────────────────────────────────────────────────────┤
│                                                      │
│                 ┌──────────────────┐                 │
│                 │                  │                 │
│                 │   3D 画布预览     │        ⛶       │
│                 │   (520px 高)     │                 │
│                 │                  │                 │
│                 └──────────────────┘                 │
│                                                      │
│  简介                                                │
│  水波涟漪效果 —— 点击方块产生基于 Morlet 小波...      │
│                                                      │
└──────────────────────────────────────────────────────┘
```

- **导航栏**：半透明背景 + 模糊，返回按钮 + 标题，顶部的 sticky
- **预览区**：圆角容器，深色背景，比首页卡片预览更大，右上角挂 GUI
- **全屏按钮**：右下角半透明，hover 发光
- **简介区**：标题 + 段落，左对齐，最大宽度限制阅读舒适度

---

## 7. 组件规范

### 按钮

- 默认：`rgba(255,255,255,0.06)` 背景 + 细边框
- Hover：背景变亮 + 边框变亮 + 轻微 scale
- 圆角 8px，过渡 0.2s ease
- 返回按钮不设背景，仅文字 + hover 下划线

### 标签

- 半透明背景 `rgba(255,255,255,0.06)`
- 圆角 6px，padding 2px 10px
- 字号 11px，颜色次要

### 卡片

- 背景 `rgba(255,255,255,0.03)`
- 边框 `rgba(255,255,255,0.06)`
- 圆角 12px
- Hover: 背景 → `rgba(255,255,255,0.06)`, 边框 → 高亮, translateY(-4px)
- 过渡 0.25s ease

---

## 8. 动效规范

| 场景 | 时长 | 缓动 | 说明 |
|------|------|------|------|
| 卡片 Hover | 250ms | ease | 上浮 + 边框变色 |
| 全屏切换 | 300ms | ease | 预览区尺寸变化 |
| 页面进入 | 400ms | ease-out | 淡入 + 上移 |
| 路由切换 | 200ms | ease-in-out | 透明度过渡 |

---

## 9. 响应式

| 断点 | 布局 |
|------|------|
| ≥ 1024px | 卡片 3 列，预览区最大宽 1000px |
| 768~1023px | 卡片 2 列 |
| < 768px | 卡片 1 列，预览区全宽，GUI 可折叠 |

---

## 10. 实现方式

使用内联 CSS 变量 + React 内联样式（或 CSS 文件），不引入第三方 UI 库。全局变量放在 `style.css`，组件内复用变量。
